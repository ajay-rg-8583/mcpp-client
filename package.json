{
	"name": "mcp-client",
	"description": "A VSCode client for MCP servers.",
	"author": "Microsoft Corporation",
	"license": "MIT",
	"version": "1.0.0",
	"repository": {
		"type": "git",
		"url": "https://github.com/Microsoft/vscode-extension-samples"
	},
	"publisher": "vscode-samples",
	"categories": [],
	"keywords": [
		"multi-root ready"
	],
	"engines": {
		"vscode": "^1.52.0"
	},
	"activationEvents": [
		"onView:mcp.chatView"
	],
	"main": "./client/out/extension",
	"contributes": {
		"configuration": {
			"title": "MCP Client",
			"properties": {
				"mcpClient.servers": {
					"type": "object",
					"description": "Configuration for MCPP servers that can be used by the extension.",
					"default": {
						"zoho-creator-mcp": {
							"type": "sse",
							"url": "http://localhost:8000/mcpp"
						},
						"zoho-crm-mcp": {
							"type": "sse",
							"url": "http://localhost:8001/mcpp"
						},
						"zoho-books-mcp": {
							"type": "sse",
							"url": "http://localhost:8002/mcpp"
						}
					}
				},
				"mcpClient.openaiApiKey": {
					"type": "string",
					"description": "The API key for the OpenAI LLM used to select the appropriate MCP server."
				}
			}
		},
		"viewsContainers": {
			"activitybar": [
				{
					"id": "mcp-client-container",
					"title": "MCP Client",
					"icon": "client/media/robot.svg"
				}
			]
		},
		"views": {
			"mcp-client-container": [
				{
					"type": "webview",
					"id": "mcp.chatView",
					"name": "MCP Chat",
					"icon": "client/media/robot.svg"
				}
			]
		}
	},
	"scripts": {
		"vscode:prepublish": "npm run compile",
		"compile": "npx tsc -p client/tsconfig.json",
		"watch": "npx tsc -p client/tsconfig.json -w",
		"lint": "eslint"
	},
	"dependencies": {
		"axios": "^0.21.1",
		"openai": "^5.5.1"
	},
	"devDependencies": {
		"@eslint/js": "^9.13.0",
		"@stylistic/eslint-plugin": "^2.9.0",
		"@types/node": "^20",
		"@types/vscode": "^1.52.0",
		"eslint": "^9.13.0",
		"typescript": "^5.8.2",
		"typescript-eslint": "^8.26.0"
	}
}
